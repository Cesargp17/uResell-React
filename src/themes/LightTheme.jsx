import { createTheme } from "@nextui-org/react"

export const defaultTheme = createTheme({
    type: 'default',
    theme: {
      colors: {
        background: '#ffffff',
        text: '#000000',

      },
      space: {},
      fonts: {}
    }
  })